package CobolJTests;

import static org.junit.Assert.*;

import java.util.ArrayList;

import org.junit.Test;
import cobol.Cobol;
import parse.tokens.Symbol;
import parse.tokens.Token;
import parse.tokens.Word;
import XMLWriter.XMLPayload;
import parse.Assembly;
import parse.Parser;
import parse.Sequence;
import parse.tokens.Tokenizer;
import cobol.CobolParser;

public class JTest {

	@Test
	public void test() {
		Word word = new Word();
		Symbol symbol = new Symbol('a');
		//Object cobol = new Clone();
		//Object assembly = new Clone();
		//ArrayList<Object> assbler = new elementsAbove(assembly, word)
		new  XMLPayload();
		new Tokenizer();
		//Sequence cobolPhraser = new cobol();
	}

}
